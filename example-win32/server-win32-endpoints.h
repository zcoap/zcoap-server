#include "../src/zcoap-server.h"

/**
 * telemetry_uri
 *
 * Example node for statically defined segments of a CoAP tree.  Some nodes
 * allow GET and PUT.  Some just allow GET.  Some return static data.  Some
 * return random data!
 */
extern const coap_node_t telemetry_uri;
