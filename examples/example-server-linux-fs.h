#include "../src/zcoap-server.h"

extern coap_code_t __attribute__((nonnull (1, 2, 3, 4))) coap_fs_gen(const coap_node_t *parent, dnode_realloc_t reallocator, size_t * const n, coap_node_t **children);

